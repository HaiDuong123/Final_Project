<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- PERMISSIONS -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Android 10 trở xuống -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />



    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Final_Project"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Activity mở đầu -->
        <activity
            android:name=".ui.trangchu.TrangChuActicvity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.ketqua.KetQuaTracNghiemActivity"
            android:exported="false" />

        <activity
            android:name=".ui.tracnghiem.ChonTracNghiemActivity"
            android:exported="false" />

        <activity
            android:name=".ui.tracnghiem.BatDauTracNghiemActivity"
            android:exported="false" />

        <activity
            android:name=".ui.ghiam.BatDauGhiAmActivity"
            android:exported="false" />

        <activity
            android:name=".ui.ghiam.HienCauHoiGhiAmActivity"
            android:exported="false" />

        <activity
            android:name=".ui.hinhanh.BatDauHinhAnhActivity"
            android:exported="false" />

        <activity
            android:name=".ui.hinhanh.ChonHinhHinhAnhActivity"
            android:exported="false" />


    </application>

</manifest>
